node_modules/
dist/
.DS_Store
.claude/

# 只发产物和造产物的代码。设计原型、技术方案、批注生产流水线（依赖不进仓库的
# 原始对话和候选池，没有那些输入也跑不起来）都留在本地。
design/
技术方案.md
tools/extract-notes.mjs
tools/clean-notes.mjs
tools/make-triage.mjs
tools/make-reader.mjs

# 个人工作笔记，不发布
Idea.txt

# 素材工作现场（含几百 MB 视频和音频、成堆论文 PDF），不进仓库。
# 每条条目自己写的那些源 —— 文稿、批注、全景图 —— 都放在 content/<条目>/ 里，
# 跟成品在一起；这些目录留下的是它们上游的原始材料：视频、ASR 返回、论文 PDF、
# 完整对话记录、中间产物、历史版本、导出的大图。
#
# 写成模式而不是逐个列：2026-08-16 把 0x_多模态/ 改名成 09_多模态/，
# 名字一变就掉出了忽略范围，下一次 git add -A 直接把 203 MB 音频和 18 篇 PDF
# 吸进了暂存区。目录名会变，「数字开头的顶层目录都是工作现场」这条不会变。
[0-9]?_*/

# 返工前的稿件备份（2026-09-01 把四期详录换成逐字精校稿时留的），
# 和上面那些工作现场同一个性质：本地留档，不进仓库。
_backup/

# 画图时开的量尺台，本地临时文件
.preview/
