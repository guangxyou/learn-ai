# learn-ai 技术方案

> 日期：2026-07-30
> 可运行原型：`design/index.html`、`design/episode.html`（双击即开，无需起服务）
> 原型用的是真数据：46 节大纲 / 154 段发言 / 4.9 万字正文 / 36 篇论文 / 36 位研究者 /
> 视频·演讲·访谈·书·延伸·报告·数据集七类资料 / 4 条编年史轨道

---

## 0. 一句话结论

**learn-ai 是一个静态站，不是一个服务。**

慧客堂需要跑 Node + SQLite，是因为它每天要跟更 99 期节目、要全文检索、要登录鉴权。
learn-ai 的内容一年可能加三五条、全部公开、几乎不变。给它配一个常驻进程，等于在那台
1.9 GB、已经跑着 4 个服务的机器上再压 200 MB 内存，换回来的只有麻烦。

所以：**构建期把 Markdown 编译成静态 HTML，nginx 直接发文件，运行时零进程。**
需要动态能力的那天（全站搜索、评论、访问统计）再加，加的时候不用推翻现在的任何东西 ——
因为内容模型是独立于渲染方式定义的。

下面四个判断是这份方案里最需要你点头或否掉的：

| # | 判断 | 影响面 |
|---|---|---|
| 1 | 静态生成，不起常驻进程 | 部署、运维、以后加搜索的方式 |
| 2 | 文稿**存 Markdown、发 HTML**（不在浏览器里解析 md） | 你以后怎么改稿 |
| 3 | 编年史**用数据重画 SVG**，PPT 原图退为兜底大图 | 编年史能做到多远 |
| 4 | 一键下载**拆成两个包**，论文 PDF 不进 git | 仓库体积、法律风险 |

---

## 1. 目标与约束

**要交付的**

- GitHub public 仓库 `learn-ai`，原始视频不进仓库
- 列表页（我深度学过的东西）+ 详情页（每条可以长得不一样），移动端 / PC 端都要好用
- 第一条 = EP117 长播客：文字稿为核心（显示字数）、大纲、播放与跳播、编年史、资料区与一键下载
- 部署到 `https://xslaoxu.cn/learn-ai`

**硬约束**

- 服务器：宝塔 + nginx + PM2，Node v18.20.8，`/www/wwwroot/`，内存紧张（已跑 4 个服务）
- nginx 全局开了 `proxy_cache cache_one` —— 但静态站走 `alias`，不经过 proxy，这个坑天然绕开了
- 子路径部署：站点跑在 `/learn-ai` 下，所有资源引用必须能带前缀

**素材现状（实测）**

| 目录 | 体积 | 能进 git 吗 |
|---|---|---|
| `video/`（B 站 1080p + 弹幕 + 封面） | 414 MB | ❌ |
| `papers/`（36 篇 PDF + 延伸资料） | 184 MB | ❌ 体积 + 再分发授权 |
| `asr/raw-transcript.json` | 9.2 MB | ⚠️ 可进，但会拖慢 clone |
| `ppt/`（51 页 PDF） | 8.0 MB | ✅ 需标注来源 |
| `asr/segments.json` | 440 KB | ✅ |
| `02-精校稿.md` / `03-资料索引.md` / `04-校订记录.md` | 200 KB | ✅ |
| `01-播客网页存档.html`（SingleFile） | 3.0 MB | ✅（内嵌 base64 头像） |

---

## 2. 内容模型：条目 + 模块装配

你的原话是「每个详情页可以不一样」。这句话决定了整个架构 ——
详情页不能是一个固定模板，它得是**一组模块按条目的声明拼出来**。

```
content/
├── entries.json                    列表页数据（每条一个摘要对象）
└── ep117-paper-journey/
    ├── entry.json                  这一条的元数据 + 模块清单
    ├── transcript.md               精校稿（= 02-精校稿.md）
    ├── index.md                    资料索引（= 03-资料索引.md）
    ├── chronicle.json              编年史四条轨道
    └── assets/                     PPT 分页图、封面
```

`entry.json`：

```jsonc
{
  "id": "ep117-paper-journey",
  "kind": "podcast",
  "title": "开源一段论文探索之旅",
  "subtitle": "模型范式、Infra 和数据、语言、多模态的完整变迁史",
  "topics": ["AI 基础", "论文", "编年史"],
  "date": "2025-10",            // 素材发生时间，精确到月
  "studied": "2026-07",         // 我学它的时间，精确到月
  "source": { "name": "张小珺Jùn｜商业访谈录 EP117", "url": "https://www.xiaoyuzhoufm.com/episode/68ff9d1b083a71a4eb86c52c" },
  "media": {
    "audio": "https://media.xyzcdn.net/626b46ea9cbbf0451cf5a962/ludOBUWHSc2Y_GA47pSOwlGC8JPS.m4a",
    "duration": 15757,
    "video": "https://www.bilibili.com/video/BV1pkyqBxEdB/"
  },
  // ↓ 详情页就是按这个数组顺序拼出来的
  "modules": [
    { "type": "transcript", "src": "transcript.md",  "label": "文稿" },
    { "type": "chronicle",  "src": "chronicle.json", "label": "编年史" },
    { "type": "resources",  "src": "index.md",       "label": "资料" }
  ]
}
```

模块类型是一个注册表：`transcript` / `chronicle` / `resources` / `gallery` / `notes` / `links`。
下一条如果是一门课，`modules` 可能是 `["notes", "links", "gallery"]`，页面自然就长得不一样，
但顶栏、播放器、主题、字号设置全部复用。**新增一种条目 = 写一个新模块，不是复制一个新模板。**

时间字段有两个（`date` / `studied`），列表页默认按 `studied` 倒序 —— 这是「我的学习史」，
不是「素材发布史」。两个都显示，避免以后自己看不懂顺序。

---

## 3. 仓库

```
learn-ai/                              github.com/guangxyou/learn-ai · public
├── README.md                          含素材来源与权利声明
├── LICENSE                            MIT（仅代码）
├── content/ep117-paper-journey/       内容（人可读、可 diff、可 PR）
│   ├── entry.json transcript.md index.md chronicle.json revisions.md
│   └── assets/AI演义-36篇论文.pdf
├── src/                               构建（纯 Node ESM，零依赖）
│   ├── parse.mjs                      Markdown → 结构化数据
│   ├── render.mjs                     结构化数据 → HTML
│   └── build.mjs                      入口：content → dist
├── public/                            app.css / app.js（浏览器只管交互）
├── scripts/deploy.sh
├── docs/nginx-learn-ai.conf
├── design/                            设计原型与说明
└── .gitignore
```

`.gitignore`：

```gitignore
node_modules/
dist/
# 大素材不进仓库，走服务器侧 assets/（见 §7.3）
content/*/media/
content/*/papers/
content/*/asr/raw-transcript.json
```

**原始素材目录 `00_论文探索之旅/` 不直接搬进仓库**，而是由一个一次性的
`scripts/ingest.ts` 把它规整成 `content/ep117-paper-journey/`。理由：原目录是工作现场
（有 `.env`、`node_modules`、临时文件、414 MB 视频），仓库要的是成品。
原目录留在本地作为归档。

---

## 4. 构建管线

### 4.1 文稿：Markdown 还是 HTML？

**存 Markdown，发 HTML。浏览器里不出现 markdown 解析器。**

理由是这三条，按重要性排：

1. **你要改稿。** 4.9 万字的精校稿一定还会改（校订记录已经有一份了）。
   Markdown 能 `git diff`、能 `git blame` 到某一句是哪次改的。HTML 做不到。
2. **HTML 才有的东西，Markdown 给不了。** 每段要挂 `data-t` 时间码用于跳播和高亮跟随、
   每节要有稳定的 `id` 用于深链、大纲要能自动生成、字数要能统计。
   这些都是**构建期解析一次**就固化下来的结构，不该在每个访客的浏览器里重算。
3. **首屏与 SEO。** 站是公开的。运行时解析 = 白屏一拍 + 爬虫拿到空 div。

现有格式已经足够解析，不需要改写稿子：

```markdown
## 6. Transformer：拉开一个时代的序幕（2017）

**谢青池**〔00:57:24〕我们终于讲到 Transformer 了……

接着上一段继续说的自然段。
```

解析规则（`src/parse/transcript.ts`，与原型里的 `extract.py` 同一套规则）：

| 输入 | 输出 |
|---|---|
| `# Part 1 …` | 章节组，正文里渲染成分隔线 |
| `## 6. Transformer…` | `<h2 id="s23">`，进大纲，取本节第一个时间码做跳播锚点 |
| `**谢青池**〔00:57:24〕正文` | 一个 turn：`{ spk, t: 3444, p: [...] }` |
| 紧随其后的普通段落 | 并入上一个 turn 的 `p[]` |
| `---` / 行内 `**粗**` `[链接]()` | 忽略 / 保留为行内 HTML |

产物：`transcript.json`（结构化，供播放同步用）+ 预渲染的 HTML 片段。
**字数**取所有 turn 正文的字符数之和，不含时间码、说话人、Markdown 标记 ——
现在这份稿子是 **48,831 字**（页面上显示「4.9 万字」）。

### 4.2 原始页存档

`01-播客网页存档.html`（SingleFile，3 MB）**不进构建管线**，作为归档留在素材目录里。

原本这里有一个 Shownotes tab，抽取也跑通了 —— 小宇宙自己的 Shownotes 结构很干净，
剥掉内嵌 base64 图与哈希 class 后只有 3 KB，还自带 `data-timestamp`。但它和这个站的
定位是重复的：Shownotes 是节目的目录，而这一页已经有 46 节的文稿大纲和四条可点的编年史，
两者说的是同一件事，后者信息还更全。所以**去掉 Shownotes tab**，
页头留一个「小宇宙原页 ↗」，需要看原始 Shownotes 和评论的人一键过去。

同理，评论也不做。索引里已经采纳的两条听众勘误（LAION 的起因是 DALL·E 不是 CLIP；
BERT 是 encoder-only）早就写进了 `03-资料索引.md`，不会因此丢失。

### 4.3 编年史

见 §5.2。数据是 `chronicle.json`，四条轨道 35 个节点，每个节点：

```jsonc
{ "date": "2017.06", "name": "Transformer", "sub": "拉开一个时代的序幕",
  "paper": 6, "section": 23 }   // → 资料区第 6 篇、文稿第 23 节
```

`paper` / `section` 两个外键是编年史能「点进去」的全部前提，构建期做一次交叉校验：
任何一个指不到的编号直接让构建失败。这比上线后发现点了没反应强。

### 4.4 资料索引

`03-资料索引.md` 结构很规整（`## Part N` → `### 序号. 名称` → `**字段**：值`，
外加若干 markdown 表格），构建期整份解析成资料区的九块内容：

| 块 | 来源 | 条数 | 解析要点 |
|---|---|---|---|
| 论文索引 | `# 论文索引` 各 `###` | 36 | 序号 / Part / 中英文名 / 作者 / 机构 / 首次发表月 / 链接 |
| 人物索引 | `## 研究者` | 36 | **只要研究者** —— 主持人、嘉宾、`### 其他被提及人物` 三块都排除 |
| 视频教程 | `## 视频教程` 表 | 9 | 只留「节目中的提及 / 整理结果」两列，去掉「状态」 |
| 演讲 | `## 演讲` 表 | 7 | 去掉「Ian Buck 2009 GTC 分享」 |
| 访谈 | `## 访谈` 表 | 5 | **补小宇宙链接与节目标题**，见下 |
| 书 / 文章 | `## 书 / 文章` 表 | 9 | 去掉「扬·盖尔的城市规划论文和著作」 |
| 延伸论文 | `### 明确点名` 表 | 9 | 表格直转 |
| 技术报告 | `### 技术报告和官方发布` 表 | 8 | 表格直转 |
| 数据集 / 工具 | `### 数据集、工具与项目入口` 列表 | 8 | **过滤掉 BooksCorpus、WebText**（索引里明说没有可用镜像，列着也点不开） |

两处需要说明的处理：

1. **访谈补链接与标题。** 索引里只写了「《商业访谈录》EP102」这样的编号。构建期从节目 RSS
   （`https://feed.xyzfm.space/dk4yh3pkpjp3`，151 期）按编号核到 eid 与完整标题，
   表格第三列从「备注」换成**节目内容**（如 EP102 →「和张祥雨聊，多模态研究的挣扎史和
   未来两年的 2 个"GPT-4 时刻"」）。五条全部核到：EP89 / EP102 / EP108 / EP115 / EP133。
   这份映射写进一个小 JSON，不靠运行时请求。
2. **日期补齐。** 36 篇全部有链接；三篇无 arXiv 的（Brook / AlexNet / DeepVideo）用会议月补齐。

> 索引里那套「与 Shownotes 标注的核对结论」（⚠️ / ℹ️）**不在页面上呈现**。
> 那是整理时的自查记录，读者不需要为几处月份差异分心；结论已经体现在页面给出的日期上。
> `03-资料索引.md` 里的原文保持不动，随时可查。

### 4.5 一键下载

**一个包，8.2 MB。**

| 文件 | 体积 | 来源 |
|---|---|---|
| `精校稿.md` | 132 KB | `02-精校稿.md` |
| `资料索引.md` | 64 KB | `03-资料索引.md` |
| `AI演义-36篇论文.pdf` | 8.0 MB | 嘉宾 PPT，51 页 |
| `论文链接.txt` | 几 KB | **构建期从索引生成**，36 行原文地址 |

文件名去掉 `01-` `02-` 这类工作序号 —— 那是我整理时的顺序，对下载的人没有意义。

**184 MB 的论文全集不做了。** 原因在 §8.1：4 Mbps 的上行扛不住，而且它本来就不是必需品 ——
36 篇的原文链接全都查证过，`论文链接.txt` 一行一个 URL，会命令行的人：

```bash
wget -i 论文链接.txt          # 或 xargs -n1 curl -O < 论文链接.txt
```

一次全拉，带宽走 arXiv 和各官方站，比从谁的服务器下都快、都权威，还永远是最新版。
不用命令行的人在资料区点单篇「原文 ↗」就行。

生成规则（`src/build.ts` 里十行）：按索引里的序号排序，每行 `# 序号 中文名` 注释 + URL，
构建期顺便校验一遍 36 条链接都非空 —— 空的直接让构建失败。

---

## 5. 页面设计

完整设计与可运行原型在 `design/`，这里只说结论和为什么。

### 5.1 视觉语言：把 PPT 的电路板搬进来

嘉宾 PPT 从第 8 页开始，用的是**电路板走线**这一套语言：墨黑的线、45° 倒角、
焊盘圆环、还有一颗 DIP 封装。这是这份材料自带的、独一无二的视觉资产，比再挑一套配色好得多。

所以：纸白底 + 墨线，强调色取阻焊层的青绿 `#0F766E`（暗色下 `#2DD4BF`）。
和慧客堂的橙色是刻意区分的 —— 两个站不该看起来像一个站的两个页面。

### 5.2 编年史：不贴图，重画

**不建议直接从 PPT 抠图贴上去**，三个理由：

1. PPT 是 960×540 的位图，手机上那些 11 px 的标注根本看不清
2. 图片是死的：不能点、不能跳播、不能连到论文、搜不到、在暗色主题下是一块白斑
3. **它藏了信息**：PPT 把节点等距排开，于是 2004 到 2012 那 8 年的空白、和 2014 年
   三篇论文挤在一个月里的密度，全都看不出来

做法是**用数据重画同一套视觉**：`chronicle.json` → 参数化 SVG。同样的走线、焊盘、
点划引线、DIP 封装，但是：

- **可点**。点一个焊盘弹卡片：日期、名称、一句话意义，三个动作 ——「读这一段」跳到文稿对应章节、
  「跳播」跳到音频那一秒、「原文 ↗」打开论文。卡片**贴着被点的焊盘出现**（水平居中对齐焊盘、
  默认落在它正下方 12px），下方装不下就自动翻到上方，永远不会被底部播放条盖住
- **两种排布**。「等距排布」还原 PPT；**「按真实年份」**把 x 轴换成真实时间轴，
  四条轨道共用同一个刻度 —— 一眼看出 2014 和 2017 有多挤、2005–2011 有多空，
  以及多模态和语言模型两条线是怎么在 2020 前后合流的。这是 PPT 给不了的东西
- **响应式**。桌面横向走线、标签上下交错；窄屏自动转成竖向导轨、标签靠右排。
  不是把大图缩小，是换一种布局

四条轨道对应 PPT 第 **8 / 26 / 32 / 41** 页（模型范式 11 个节点、Infra 与数据 7 个、
语言模型 8 个、多模态 9 个），第 7 页那张「四个要素 + 四条线」的总览图作为编年史页的开场。
页面上不再提「看 PPT 原图」—— 重画的版本信息更全也更好用，把人引回一张看不清的位图没有意义；
PPT 完整版在页头「嘉宾 PPT ↗」和下载区里都能拿到。

### 5.3 详情页：文稿是主角

标签页三个：**文稿** · 编年史 · 资料。

文稿这一屏的设计要点：

- **左侧常驻大纲**（46 节，按 Part 分组），播放时当前节高亮；窄屏收进「目录」抽屉
- **正文占满大纲右边的整条列**（约 860px），不留空白栏。CJK 长文不需要刻意收窄字段宽度，
  行距和段距已经把节奏定住了
- **每段左侧一个时间码**，`00:00:00` 定长格式，点一下跳播。播放时当前段落整段高亮、
  可跟随滚动（可关）
- **阅读设置**：字号三档（默认中）/ 黑体·宋体 / 时间码显示隐藏，存 localStorage。
  4.9 万字是要坐下来读的量，宋体那一档不是装饰
- 顶部标签上直接写「**4.9 万字**」—— 你要的字数，放在最显眼的地方
- 正文里的三级小标题、有序表、无序表都按 Markdown 的语义渲染（这份稿子里有 15 个 `###`、
  9 处列表），不能当普通段落甩出去
- 空格键播放暂停、← → 前后跳

播放器常驻页面底部，跨 tab 不中断：在编年史里点「跳播」、在文稿里点时间码，
音频都会跳过去，文稿那边的高亮跟着走。控件布局照搬慧客堂：**−15 / ▶ / +30 三个图标按钮**
挨在一起（±秒在播放键左右）；最左边放**这期节目在小宇宙上的封面图**
（走 `?imageMogr2/thumbnail/160x160`，18 KB，不是原图那 1.8 MB）。

### 5.4 列表页

刻意做到几乎没有东西：顶栏（logo + `learn-ai` + slogan）+ 条目列表，没别的。
没有筛选条、没有站点统计、没有说明文字 —— 一个只有几条内容的站，那些都是噪音，
等条目多到需要筛选时再加。

一行一条：**学习时间（`2026.07`，精确到月）** + 主题标签 + 标题 + 一句话 + 产出物徽章
（「文稿 4.9 万字」「论文 36 篇」「PPT 51 页」）。
产出物徽章是这个列表页跟一个普通博客归档的区别 —— 它一眼告诉你**这条学到了什么程度**，
所以只放能量化、且对读者有意义的东西。

**Logo 与 slogan** 围绕「跟上 AI 时代」出了 6 × 6 个候选，
在 `design/pick.html` 里可以直接看效果（左边大图、右边真实顶栏里的样子）。
定稿是 **`S1 一篇一篇，跟上 AI 时代`** + **`L1 追赶`**（虚线走在前面，实线从后面追上来，
末端一个实心焊盘）—— 主题就写在图形里。

### 5.5 主题

**不做深浅色切换，只发亮色。** 一个内容站的主题开关是伪需求：它占一个顶栏位置、
要存一份状态、每加一个组件都要多测一遍暗色。CSS 里的颜色仍然全部走变量，
哪天真想要暗色，加一段 `@media (prefers-color-scheme: dark)` 就够，不用改结构。

### 5.6 移动端

不是「桌面版缩小」，三处是真的换了布局：编年史（横→竖）、文稿大纲（侧栏→抽屉）、
资料区（多栏→单栏）。播放器在移动端收成一条 mini bar（logo 与时间码让位给控件），
`env(safe-area-inset-bottom)` 处理刘海屏。

---

## 6. 播放与跳播

**音频直接用小宇宙的 CDN，已实测可用：**

```
$ curl -sI -r 0-1 https://media.xyzcdn.net/626b46ea9cbbf0451cf5a962/ludOBUWHSc2Y_GA47pSOwlGC8JPS.m4a
HTTP/2 206          ← 支持 Range，跳播不用下整个文件
accept-ranges: bytes
x-oss-cdn-auth: success
```

带不带 `Referer` 都返回 206，**没有防盗链**。`<audio>` 不设 `crossOrigin` 就不需要 CORS。

风险与兜底：这个 URL 是小宇宙的资源，哪天失效不在我们控制内。
所以 `entry.json` 里 `media.audio` 是可替换的一个字段，兜底方案按成本排序：

1. 服务器上放一份 m4a（约 120 MB），nginx 直接发，`media.audio` 改成 `/learn-ai/media/ep117.m4a`
2. nginx 反代 `location /learn-ai/media/ { proxy_pass https://media.xyzcdn.net/...; }`

建议**上线就用方案 1 的备份文件放着但不启用**，页面用 CDN，加一个每周探活的 cron，
探测失败就切。这件事的成本是一个 120 MB 的文件和 10 行脚本，不值得省。

---

## 7. 部署

### 7.1 nginx

静态站，`alias` 直接发文件，不经过 `proxy_pass` —— 全局 `proxy_cache cache_one`
那个坑（慧客堂和 Sankey 都踩过）在这里天然不存在。

```nginx
# 加到 xslaoxu.cn 的 server 块里

location = /learn-ai { return 301 /learn-ai/; }

location ^~ /learn-ai/ {
    alias /www/wwwroot/learn-ai/dist/;
    try_files $uri $uri/index.html $uri.html =404;

    # 公开站，允许收录（与 /podcast/huiketang 的 noindex 相反）
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-Content-Type-Options "nosniff" always;

    # 带指纹的静态资源长缓存
    location ~* /learn-ai/assets/.*\.(css|js|png|jpg|svg|woff2)$ {
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
    }
    # HTML 不缓存，改了就能看到
    location ~* /learn-ai/.*\.html$ {
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Cache-Control "no-cache" always;
    }
    # 下载包
    location ~* /learn-ai/download/ {
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Cache-Control "public, max-age=604800" always;
    }
}
```

> **两个坑照抄慧客堂的经验：**
> ① nginx 的 `add_header` **不继承** —— 子 location 里一出现任何 `add_header`，
> 父级的全部失效。所以上面每个子块都把两行安全头重复了一遍。
> ② `alias` 的路径**必须以 `/` 结尾**，且 `location` 也要以 `/` 结尾，否则会拼出错误路径。

### 7.2 构建产物中的路径前缀

全站跑在 `/learn-ai` 下，所有生成的链接统一走一个 `url()`（跟慧客堂 `config.ts` 一个思路）：

```ts
export const BASE = process.env.BASE_PATH ?? '/learn-ai';
export const url = (p: string) => (BASE + p).replace(/\/+/g, '/');
```

本地预览 `BASE_PATH=` 置空，`npx serve dist` 就能看。

### 7.3 服务器目录

```
/www/wwwroot/learn-ai/          ← git checkout
├── dist/                       ← 构建产物，nginx 发这里
├── content/ src/ public/ …
└── assets-private/             ← 不在 git 里，rsync 上来（一次）
    └── media/ep117.m4a         120 MB（音频兜底，默认不启用）
```

`资料包.zip` 只有 8.2 MB，构建期直接打进 `dist/download/`，服务器上不需要额外的大文件。
`assets-private/` 现在只剩那份兜底音频。

### 7.4 部署流程

沿用慧客堂的形状（服务器上 `git pull` + 本地构建），但**没有 PM2 常驻**：

```bash
# scripts/deploy.sh —— 在服务器上执行
git fetch origin main -q && git reset --hard origin/main -q
node src/build.mjs                 # 零依赖，不需要 npm install
chown -R www:www dist
curl -fsS -o /dev/null https://xslaoxu.cn/learn-ai/ && echo "[deploy] done"
```

构建是纯 CPU、几秒钟的事，**不需要常驻进程，也不需要 max_memory_restart**。
构建失败时 `dist/` 还是旧的，线上不受影响 —— 这是静态站白拿的一个好处。

nginx 那边只要在 `node_GoldArbitrage.conf`（xslaoxu.cn 的 server 块）里加一行 include，
和 ProductAnalytics / Sankey / 慧客堂 的做法完全一致：

```nginx
include /www/wwwroot/learn-ai/docs/nginx-learn-ai.conf;
```

### 7.5 GitHub

- 仓库名 `learn-ai`，public，MIT（代码）+ CC-BY-4.0（我写的内容）
- README 里必须写清楚**素材来源与权利**：节目版权属张小珺工作室，PPT 属嘉宾谢青池
  （节目里明确说了「开源给大家」，飞书链接在 Shownotes 里），文字稿是我方转写精校，
  论文版权属各自作者。**提供一个联系方式和「收到通知即下架」的声明** —— 公开站这一步不能省
- **零依赖**：`src/` 是纯 Node ESM，没有 `node_modules`，服务器上 `git pull && node src/build.mjs`
  就完事，不用 `npm install`。这在一台内存紧张的机器上很值
- 部署走服务器侧 `git pull`，不用 GitHub Actions —— 国内服务器从 GH Actions 拉取不稳定

---

## 8. 下载托管与数据统计

### 8.1 下载放哪：全在自己服务器，只有一个 8.2 MB 的包

服务器实测规格：**4 Mbps 上行 / 300 GB 月流量**。这两个数把方案定死了。

**流量完全够，个位数 GB 就够用：**

| 项 | 单次 | 一个月 |
|---|---|---|
| 页面（HTML+CSS+JS，gzip 后） | ~100 KB | 1 万 PV ≈ 1 GB |
| `资料包.zip` | 8.2 MB | 500 次 ≈ 4 GB |
| 音频（走小宇宙 CDN） | 0 | 0 |

**带宽是硬约束。** 4 Mbps = 500 KB/s，全站上行就这么多。所以原来那个 184 MB 的
论文全集包**不做了** —— 一个人下要 6 分多钟，这 6 分钟里 learn-ai、慧客堂、
`/analytics`、Sankey 全都跟着卡。为一个「打包方便」的功能，代价太大。

替代方案不花任何带宽：资料包里放一个 `论文链接.txt`（见 §4.5），36 篇的原文地址，
`wget -i` 一次全拉，流量走 arXiv 和各官方站。资料区每篇也都有「原文 ↗」。

也不用 GitHub Releases。它能解决带宽，但代价是：184 MB 的 asset 从国内传上去慢且易断、
国内下载体验不稳定、页面上多一个让读者做选择的链接、下载计数还得跨两处合并 ——
而 `论文链接.txt` 把这些问题一次都绕开了。**GitHub 仓库本身更放不下**：
单文件硬限制 100 MB，Git LFS 免费额度只有 1 GB/月流量，下五次就爆。

nginx 按 4 Mbps 收紧：

```nginx
# http 块里先声明一次
# limit_conn_zone $binary_remote_addr zone=dl:10m;

location ~* ^/learn-ai/download/ {
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Cache-Control "public, max-age=604800" always;

    limit_rate_after 2m;    # 前 2 MB 全速，小包几乎无感
    limit_rate 256k;        # 之后压到半条管子，站还能用
    limit_conn dl 1;        # 一个 IP 同时只能拉一个
}
```

8.2 MB 的包：前 2 MB 瞬间，剩下 6.2 MB 约 24 秒，全程只占一半上行。

**服务器上不再需要 `assets-private/papers/`**（§7.3 里那 184 MB 可以不传），
只剩兜底音频那 120 MB。`scripts/pack.sh` 也简化成只打一个 zip，构建期就能完成，
不再依赖服务器上的大文件。

### 8.2 统计：已接入自建的 `/analytics`

服务器上本来就跑着 ProductAnalytics（`https://xslaoxu.cn/analytics/`，PM2 端口 8093，
JSONL 存储）。learn-ai 作为第四个产品接了进去 —— 没装 Umami、没加 GA、没多起一个进程，
和「静态站零运行时」不冲突：静态页面只是往一个已有接口打一条 POST。

**ProductAnalytics 侧改了四处**（已上线，commit `bfaaf61`）：
`src/analytics.ts` 的类型、`src/server.ts` 的 `PRODUCTS` 白名单（不在里面会被 400）、
`public/analytics.js` 的 `productNames`、`public/index.html` 的产品下拉。

**learn-ai 侧在 `public/app.js` 顶部**，约 30 行：

```js
const VID = localStorage['la-vid'] ||= crypto.randomUUID();   // 本地随机串，不是 cookie
function track(event, extra = {}) {
  const body = JSON.stringify({ product: 'learn_ai', event, visitor_id: VID,
    path: location.pathname, referrer: document.referrer, ...extra });
  navigator.sendBeacon('/analytics/api/events', new Blob([body], { type: 'application/json' }));
}
```

`sendBeacon` 在页面跳走 / 开始下载时也不会被打断；取不到 localStorage（隐私模式）就回落成
服务端的 `hash(ip|ua)`。所有 track 调用都包在 try/catch 里 —— 统计失败绝不能影响页面。

采集这几件事：

| 事件 | 什么时候 | 回答什么问题 |
|---|---|---|
| `page_view` | 进页面 | PV / UV |
| `tab_view` | 切 tab | 文稿、编年史、资料哪个被点得多 |
| `read_progress` | 文稿滚过 25/50/75/100% | **4.9 万字有没有真被读** |
| `play_started` | 第一次点播放 | 有多少人真的听 |
| `chronicle_node` | 点编年史节点 | 哪几篇论文最被关心 |
| `download_started` | 点资料包 | 下载量 |

事件名沿用它的既有约定（`*_started` 会被看板认成动作漏斗）。

**兜底核对用 nginx 日志。** 埋点会被广告拦截插件和直接粘 URL 的人漏掉，
真实下载次数以 access log 为准：

```bash
awk '$9 ~ /^(200|206)$/ && $7 ~ /learn-ai\/download\//' /www/wwwlogs/GoldArbitrage.log \
  | awk '{print $1, $7}' | sort -u | wc -l    # 按 IP+文件去重，避免断点续传重复计数
```

注意 206（断点续传）会让「请求数」远大于「下载人数」，一定要按 `IP + 文件` 去重再数。

**隐私**：不引任何第三方脚本，不放 cookie，visitor id 是浏览器本地生成的随机串，
服务端只存它的 hash。

---

## 9. SEO 与隐私

站是公开的，和慧客堂（`noindex` + 单密钥登录）完全相反：

- 每条条目输出 `<meta description>`、OG 卡片、`JSON-LD`（`PodcastEpisode` / `Article`）
- `sitemap.xml` + `robots.txt` 构建期生成
- 文稿 4.9 万字全部在 HTML 里 —— 这是这个站最好的 SEO 资产，不要懒加载
- **不加任何第三方统计脚本**（不用 GA / 百度统计）。数据走自己的 `/analytics`，见 §8.2
- 存档 HTML 里那些 base64 头像与配图**一律不搬进来**（3 MB 里大半是它们，而且是别人的图）
- 播放条的封面图直接引小宇宙 CDN，加 `referrerpolicy="no-referrer"`

---

## 10. 风险与待确认

| # | 事项 | 状态 / 建议 |
|---|---|---|
| 1 | 论文 PDF 再分发 | **不再托管任何论文 PDF**，只给原文链接（`论文链接.txt` + 页面直链）。授权风险直接归零，顺带省掉 184 MB 磁盘和全部下载带宽 |
| 2 | PPT 再分发 | 嘉宾已公开飞书链接，页面上已同时给飞书原链，我们只托管 PDF 与分页图 |
| 3 | 音频 CDN 失效 | 已实测可用（206 + Range + 无防盗链）；备份 m4a 放服务器，加每周探活 |
| 4 | 精校稿字数 | 原始 ASR 87,504 字 → 精校后 48,831 字（去口语 + 合并）。页面显示精校稿字数，是对的；想同时展示「原始 8.8 万 → 精校 4.9 万」也是个不错的信息 |
| 5 | 第 7 页总览图的四条线 | 与四条轨道详图的节点集合一致，已核对 |
| 6 | 中文宋体 | Android 上可能没有可用宋体而回落到黑体。**不引 Web Font**（思源宋体子集也要 2 MB+），接受回落 |
| 7 | Shownotes 与评论 | 已决定都不做，页头「小宇宙原页 ↗」是入口。抽取脚本跑通过，存档 HTML 留在归档目录里，随时可以捡回来 |

---

## 11. 上线记录

| | |
|---|---|
| 仓库 | https://github.com/guangxyou/learn-ai · public |
| 站点 | https://xslaoxu.cn/learn-ai |
| 统计 | https://xslaoxu.cn/analytics（产品筛选里选 learn-ai） |
| 服务器 | `/www/wwwroot/learn-ai`，nginx include 一行，无常驻进程 |
| 上线内容 | 46 节大纲 / 154 段发言 / 48,831 字文稿 / 36 篇论文 / 36 位研究者 / 4 条编年史轨道 |

**日常更新的动作**：改 `content/` 里的 Markdown → 提交推送 → 服务器上跑
`bash scripts/deploy.sh`。构建期会重新解析、重新校验交叉引用、重打下载包。

**加一个新条目**：在 `content/` 下新建一个目录，写 `entry.json` 声明它要哪些模块，
放对应的素材文件。列表页和 sitemap 自动带上，不用改任何代码。

---

## 附：与慧客堂技术栈的对照

| | 慧客堂 | learn-ai |
|---|---|---|
| 运行时 | Node 18 + Express + PM2 常驻 | **无**，nginx 发静态文件 |
| 数据 | SQLite（better-sqlite3 + trigram FTS） | JSON / Markdown 文件 |
| 渲染 | 服务端模板 `views/pages.ts` | 同一套模板，**构建期**跑一次 |
| 鉴权 | 单密钥 + HMAC cookie，全站 noindex | 全公开，鼓励收录 |
| 更新 | 每天 04:20 cron 跟更 RSS | 手动，一年几次 |
| 内存 | 220 MB 上限 | 0 |
| 代码复用 | — | `layout.ts` 的骨架、`config.ts` 的 `url()`、`deploy.sh` 的形状都直接借过来 |

技术选型的差异不是为了不一样，是因为**这两个站的更新频率差了三个数量级**。
真到了需要动态能力的那天（比如加全站搜索），把同一套 `views/` 挂到 Express 上就行 ——
这也是为什么模板要和构建脚本分开写。
